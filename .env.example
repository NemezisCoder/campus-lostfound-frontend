# Public client-side routing values only. Do not put secrets in VITE_* variables.
VITE_API_URL=/api/v1
# Leave empty in production so Socket.IO uses the current origin through the reverse proxy.
VITE_SOCKET_URL=
# Optional absolute backend origin for media paths when not served from the same origin.
VITE_API_ORIGIN=
